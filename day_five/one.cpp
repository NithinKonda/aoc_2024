using namespace std;
#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <algorithm>

int main() {
    string s;
    while(getline(cin,s))
    {
        if(s.empty() || !isdigit(s[0])){
            continue;
        }

        int len = (int) s.length();
    }

}